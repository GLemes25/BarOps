Refatore a renderização dos cards no EventDrinksSection para criar uma separação física e visual estrita entre os drinks selecionados e os não selecionados, utilizando uma linha divisória estilizada.

Execute rigorosamente os seguintes passos de lógica e layout:

Separação dos Arrays:

Em vez de apenas ordenar a lista inteira com .sort(), divida os availableDrinks filtrados (apenas os que correspondem à busca) em dois arrays independentes:

const selectedDrinks = filteredDrinks.filter(drink => ...); (utilizando a lógica de seleção de independentes e pastas anterior).

const unselectedDrinks = filteredDrinks.filter(drink => !...);

Ordene ambos os arrays de forma alfabética (A-Z) individualmente usando localeCompare.

Renderização do Bloco Superior (Selecionados):

Se selectedDrinks.length > 0, renderize uma <div> contendo um subtítulo sutil (ex: 'Drinks no Cardápio') seguido da grade (grid gap-4 sm:grid-cols-2 lg:grid-cols-3) contendo apenas os cards selecionados.

A Linha Divisória Estilizada:

Se ambos os arrays (selectedDrinks e unselectedDrinks) possuírem itens, renderize uma linha divisória entre eles.

Use uma estrutura minimalista e elegante em Tailwind com texto centralizado:

TypeScript

<div className="flex items-center my-8 text-xs font-medium uppercase tracking-wider text-muted-foreground before:h-px before:flex-1 before:bg-border before:mr-4 after:h-px after:flex-1 after:bg-border after:ml-4">
  Opções Disponíveis no Catálogo
</div>
Renderização do Bloco Inferior (Não Selecionados):

Se unselectedDrinks.length > 0, renderize a grade contendo apenas os cards que ainda não foram adicionados ao evento.

Caso de Lista Vazia:

Se a busca não retornar nenhum resultado em nenhum dos blocos, exiba a mensagem amigável de 'Nenhum drink encontrado'.

Mantenha todos os comportamentos de clique (toggleDrink), estados de loading e responsividade intactos. Não adicione comentários explicativos no código.
