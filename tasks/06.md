"Instale os componentes necessários para as tabelas e modais: pnpm dlx shadcn-ui@latest add table dialog dropdown-menu label.
Crie um layout padrão para as páginas de listagem. A estrutura visual esperada para qualquer página (ex: /drinks) é:

Um cabeçalho no topo com um título (H1) à esquerda e um botão principal 'Novo Registro' à direita.

Ao clicar em 'Novo Registro', deve abrir um componente Dialog do shadcn. Dentro do Dialog, renderizaremos o formulário (React Hook Form + Zod).

O corpo da página deve exibir um componente Table do shadcn com os dados.

Na última coluna da tabela de cada registro, deve haver um botão de 'Ações' usando DropdownMenu do shadcn, contendo as opções 'Editar' (que abrirá o formulário preenchido em um Dialog) e 'Excluir'."
