"Eu encontrei um erro estrutural na função onSubmit de todos os formulários dentro da pasta components/forms/. Atualmente, eles estão ignorando os dados do formulário (\_values) e apenas chamando onSuccess().

Sua tarefa é iterar sobre TODOS os arquivos de formulário existentes em components/forms/ (ex: DrinkForm, EventForm, LaborForm, MaterialForm, etc.) e aplicar a seguinte correção:

Imports: Para cada formulário, importe as respectivas Server Actions de criação e atualização (assuma o padrão @/actions/[nome]-actions, ex: createEvent, updateEvent). Se os arquivos de actions ainda não existirem, você pode assumir que eles existirão com essa nomenclatura.

Refatoração do onSubmit: Altere a assinatura de async (\_values: Tipo) => para async (values: Tipo) => (remova o underscore).

Lógica de Salvamento: Implemente um bloco try/catch.

Dentro do try, verifique se record?.id ou record.id existe.

Se existir, faça await updateEntidade(record.id, values);.

Se não existir, faça await createEntidade(values);.

A chamada para onSuccess() deve acontecer APENAS no final do bloco try, após as operações de banco de dados concluírem com sucesso.

Tratamento de Erro: No bloco catch, adicione um console.error simples por enquanto.

Respeite estritamente as regras de TypeScript e Clean Code definidas em .claude/rules. Não remova os schemas do Zod nem a estrutura do react-hook-form. Apenas conserte a lógica de submissão."
