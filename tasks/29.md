Adicionei a coluna parentId: integer('parent_id').references((): AnyPgColumn => drinks.id) na tabela drinks para implementar o padrão de Variações de Produto (Parent/Child). Isso servirá para agrupar variações como 'Caipiroska' e 'Saquerinha' sob um drink pai 'Caipifruta de Limão'.

Como um Arquiteto de Software Sênior, implemente essa lógica em todo o sistema sem quebrar os dados atuais:

Server Actions (actions/drink-actions.ts etc):

Atualize as queries de busca de drinks. Quando buscar a lista de drinks, agrupe-os: retorne os drinks principais (onde parentId é null) e, junto deles, um array aninhado variants contendo os drinks filhos que apontam para aquele pai.

Gerenciamento de Drinks (CRUD app/drinks/...):

Na tabela/lista de drinks, exiba um indicativo visual de que um drink possui variantes (ex: um accordion ou um badge '3 variações').

No formulário de criar/editar Drink (drink-form.tsx), adicione um select opcional chamado 'É variação de outro drink? (Drink Pai)'. Isso permite que o usuário pegue um drink existente e o transforme numa variação de outro.

Planejamento do Evento (app/events/[id]/...):

Ao adicionar um drink ao evento (event-drinks-form ou modal de seleção), mude a UX:

Se o usuário selecionar um drink que possui variantes (filhos), mostre imediatamente um grupo de checkboxes (ex: Vodka, Cachaça, Sem Álcool) referentes aos nomes dos drinks filhos.

O usuário pode selecionar múltiplas variações. O sistema deve salvar na tabela event_drinks os IDs individuais das variantes escolhidas, e NÃO o ID do pai (pois o pai não tem a receita, os filhos têm).

Resiliência:

Os dados atuais possuem parentId: null. O sistema deve continuar permitindo que drinks sem pai sejam adicionados normalmente aos eventos.

Não adicione comentários no código, foque na refatoração limpa e UX premium com shadcn/ui."
