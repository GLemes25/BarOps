"Estou com um problema de reatividade no sistema. Quando eu crio, edito ou excluo um registro, a lista na tela não atualiza automaticamente, exigindo um recarregamento manual da página.

Sua tarefa é corrigir a sincronização de estado e o cache do Next.js em todos os fluxos de CRUD (events, drinks, ingredients, labor, materials). Execute os seguintes passos rigorosamente:

Atualização das Server Actions (src/actions/\*-actions.ts):

Varra todos os arquivos de actions.

Certifique-se de importar revalidatePath de next/cache.

No final de TODAS as funções de mutação (create, update, delete), adicione a chamada do revalidatePath apontando para a rota principal daquela entidade. Exemplo: Se for uma action de drink, adicione revalidatePath('/drinks') imediatamente antes do return de sucesso.

Sincronização de Estado no Frontend (app/\*_/_-table.tsx):

Varra todos os componentes de tabela (drinks-table.tsx, ingredients-table.tsx, events-table.tsx, etc.).

Importe o hook useEffect do React.

Logo abaixo da declaração do estado (const [dados, setDados] = useState(initialData)), adicione um useEffect que observe a prop initialData.

O useEffect deve atualizar o estado local sempre que a prop mudar. Exemplo:
`useEffect(() => {
  setDados(initialData);
}, [initialData]);`
Atualização da Tabela após Salvar (app/\*\*/page.tsx ou formulários):

Confirme que os modais (Dialogs) estão sendo fechados e, ao mesmo tempo, chame router.refresh() do next/navigation na função onSuccess ou no fechamento do modal no componente -table.tsx.

A função handleDelete já deve possuir router.refresh() após o await da action.

Não gere comentários no código, apenas aplique as correções e salve os arquivos."
