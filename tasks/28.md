"Como um Engenheiro Next.js Sênior, preciso refatorar a 'Lista de Compras' de uma rota separada para uma Aba (Tab) nativa dentro da página de detalhes do evento.

Execute os seguintes passos rigorosamente:

Criação do Componente (app/events/[id]/shopping-list-section.tsx):

Extraia a UI do atual app/events/[id]/shopping-list/page.tsx para este novo arquivo.

O componente não deve ser async nem fazer fetch de dados. Transforme-o em um componente normal que recebe shoppingList e event através das Props.

Remova o botão de voltar (Voltar para o Evento), pois já estaremos na tela do evento.

Mantenha o botão de impressão (PrintButton).

Mantenha a propriedade suppressHydrationWarning em todos os locais que formatam data ou moeda.

Atualização da Página do Evento (app/events/[id]/page.tsx):

Localize a estrutura do <Tabs> (do shadcn/ui).

No <TabsList>, adicione um novo <TabsTrigger value="compras">Lista de Compras</TabsTrigger>.

Adicione o respectivo <TabsContent className="mt-4" value="compras">.

Renderize o novo <ShoppingListSection event="{event}" shoppingList="{shoppingList}"/> dentro deste TabsContent (como o componente pai já faz o fetch desses dados, basta repassá-los).

Atualização do EventFinancialSection:

Como a lista de compras agora é uma aba irmã, o link de redirecionamento não faz mais sentido.

Na TableCell que diz 'Ingredientes (Lista de Compras)', remova o <Link> (Ver Lista de Compras) e o ícone do carrinho. Deixe apenas o texto 'Ingredientes (Lista de Compras)'.

Limpeza:

Após concluir as integrações, exclua a pasta app/events/[id]/shopping-list e seu conteúdo, pois a rota separada não deve mais existir.

Faça as alterações de forma limpa, garantindo a tipagem do TypeScript e sem adicionar comentários explicativos no código."
Não faça o commit até ue mandar e adicione isso nas regras .claude/rules
