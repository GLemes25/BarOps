"Modifique o app/page.tsx para ser o Dashboard inicial do sistema. Limpe o conteúdo anterior.
Adicione cards de resumo (use pnpm dlx shadcn-ui@latest add card) para exibir indicadores mockados por enquanto, como: 'Total de Eventos', 'Receita Estimada', 'Bebidas Mais Pedidas'. Mantenha o design limpo e alinhado com o tema escuro do projeto."
"Refatore o Dashboard (app/page.tsx) para consumir dados reais do banco de dados (Neon DB + Drizzle) através de Server Actions, substituindo os dados mockados atuais. Siga rigorosamente as regras definidas em .claude/rules/ (geral.md, typescript.md e react.md).

Passo a Passo:

Ajuste de Schema: Verifique em db/schema.ts se existe uma tabela que relacione eventos e bebidas (ex: event_drinks com event_id e drink_id). Se não existir, crie essa tabela de junção para sabermos quais drinks estão em quais eventos, e atualize o banco de dados (drizzle-kit push ou gere a migration).

Server Actions (Dashboard): Crie o arquivo src/actions/dashboard-actions.ts. Crie e exporte uma função assíncrona chamada getDashboardStats que utilize o Drizzle para retornar:

totalEvents: Um count da tabela events.

estimatedRevenue: A soma de todos os custos (mão de obra + materiais) de todos os eventos (ou um valor de orçamento total, dependendo de como estruturou os custos do evento).

topDrink: Uma query que agrupe a tabela event_drinks, faça um count por drink_id, faça o join com a tabela drinks e retorne o nome da bebida mais frequente.
Atenção: Trate possíveis valores nulos ou vazios caso não haja eventos cadastrados.

Refatoração do Page (React): > - Altere app/page.tsx para ser um Server Component assíncrono.

IMPORTANTE: Siga a regra de usar Arrow Function (const DashboardPage = async () => { ... }) e exporte-o como default.

Chame a action getDashboardStats() no corpo do componente.

Substitua o array estático summaryCards por variáveis dinâmicas alimentadas pelos dados da action.

Formate o valor de estimatedRevenue para a moeda BRL (R$) nativamente via Intl.NumberFormat.

Certifique-se de manter os imports do lucide-react e os componentes do shadcn/ui (Card) perfeitamente intactos e de não deixar nenhum comentário no código gerado."
