Precisamos evoluir o módulo de Ingredientes para suportar 'Sub-receitas' ou 'Pré-preparos' (ex: Espuma de Gengibre, que é feita de outros ingredientes).

Execute rigorosamente os passos abaixo seguindo nossas .claude/rules:

Atualização do Schema (db/schema.ts):

Na tabela ingredients, adicione: isSubRecipe: boolean("is_sub_recipe").default(false).notNull().

Altere purchaseUnit e purchaseCost para poderem ser nulos (remova o .notNull()), pois sub-receitas não são compradas diretamente.

Crie a tabela ingredient_components com: id (serial), parentId (integer referenciando ingredients.id), childId (integer referenciando ingredients.id), e quantity (decimal precision 10, scale 4).

Faça o push para o banco de dados (drizzle-kit push).

Refatoração das Actions (actions/ingredient-actions.ts):

Atualize getIngredients para trazer junto os components usando o .with() do Drizzle.

Atualize createIngredient e updateIngredient: Se o objeto vier com um array components, use uma transação. Salve/atualize o ingrediente principal e insira/recrie as linhas na tabela ingredient_components. Calcule o purchaseCost da sub-receita somando os custos dos filhos proporcionais à receita.

Refatoração do Formulário (app/ingredients/ingredient-form.tsx):

O componente deve receber uma nova prop: availableIngredients: IngredientRecord[] (para o select dos filhos).

Atualize o schema do Zod: adicione isSubRecipe (boolean), torne purchaseUnit e purchaseCost opcionais se isSubRecipe for true. Adicione o array components (semelhante ao de drinks).

Na interface, adicione um Checkbox (@/components/ui/checkbox) "Este ingrediente é um pré-preparo/sub-receita?".

Se o checkbox estiver MARCADO:

Esconda os inputs de purchaseUnit e purchaseCost.

O label de yieldQuantity deve mudar para 'Rendimento da Receita (em Un. Receita)'.

Renderize um useFieldArray idêntico ao do drink-form, permitindo selecionar ingredientes filhos e suas quantidades.

Atualização da Tabela (app/ingredients/ingredients-table.tsx):

Receba availableIngredients e repasse para o formulário no Dialog.

Na exibição visual, adicione um Badge 'Pré-preparo' ao lado do nome se isSubRecipe for true.

O Motor da Lista de Compras (actions/event-actions.ts):

Na action getEventShoppingList, atualize a lógica para "desempacotar" sub-receitas.

Se um ingrediente necessário no evento for isSubRecipe === true, o sistema não deve colocá-lo na lista de compras final. Em vez disso, ele deve calcular quantas 'receitas' são necessárias (Total Necessário / yieldQuantity) e adicionar os ingredientes 'filhos' multiplicados por esse multiplicador à lista geral antes de fazer o cálculo final de embalagens.

Não crie comentários no código e garanta as tipagens.
