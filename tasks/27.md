"Adicionei as colunas status (enum: planning, confirmed, completed, canceled) com default 'planning' e revenue (decimal) com default '0' na tabela events do meu schema.

Como um Desenvolvedor Fullstack Sênior, atualize o sistema rigorosamente com as seguintes instruções, garantindo que nada quebre:

Server Actions (actions/event-actions.ts / actions/get-event-by-id.ts):

Atualize os schemas Zod de criação e edição para aceitar status (string/enum) e revenue (coerce number).

Garanta que as tipagens de retorno e inserts contemplem esses novos campos.

Formulário de Evento (app/events/event-form.tsx):

Adicione um select para status (opções: Planejamento, Confirmado, Concluído, Cancelado).

Adicione o campo de Receita (revenue). Para melhorar a UX, adicione um state local revenueType ('total' ou 'per_person'). Se o usuário selecionar 'per_person', o valor digitado no input de receita deve ser multiplicado pelos guests (em tempo real ou no onSubmit) antes de ser enviado para a Server Action. No formulário de edição, se houver receita, assuma 'total' como default visual.

Interface do Financeiro (EventFinancialSection):

Atualize o componente para receber a prop revenue: number.

Crie constantes: profit = revenue - grandTotal e margin = revenue > 0 ? (profit / revenue) \* 100 : 0.

Adicione dois novos Cards em destaque na interface:

Receita Total (Cobrado): exibe o revenue.

Lucro Líquido: exibe o profit (use formatação de moeda e, se possível, cores condicionais: verde para lucro, vermelho para prejuízo). Mostre a margin em % como CardDescription.

Tabela de Eventos (app/events/events-table.tsx):

Adicione uma coluna para exibir o Status usando um Badge do shadcn (ex: cinza para planejamento, verde para confirmado, azul para concluído, vermelho para cancelado).

Faça as alterações de forma limpa, não crie comentários explicativos no código.
