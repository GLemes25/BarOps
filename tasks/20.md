"Precisamos adicionar a interatividade nas seções de detalhes do evento (app/events/[id]/). O usuário deve poder adicionar Drinks, Mão de Obra e Materiais ao evento, e navegar para a Lista de Compras a partir do resumo financeiro.

Execute rigorosamente os seguintes passos, respeitando as .claude/rules (shadcn, react-hook-form, zod, next/navigation):

Criação dos Formulários de Alocação (Co-localizados em app/events/[id]/):

Crie event-drink-form.tsx: Deve receber eventId e availableDrinks (catálogo). O formulário (Zod) terá apenas drinkId. No submit, chame a action addDrinkToEvent(eventId, drinkId) e feche o modal.

Crie event-labor-form.tsx: Deve receber eventId e availableLabor (catálogo). O formulário terá laborCatalogId e quantity. No submit, chame a action addLaborToEvent(eventId, laborCatalogId, quantity) e feche o modal.

Crie event-material-form.tsx: Deve receber eventId e availableMaterials (catálogo). O formulário terá materialCatalogId e quantity. No submit, chame a action addMaterialToEvent(eventId, materialCatalogId, quantity) e feche o modal.
Atenção: Se essas actions ainda não existirem em src/actions/event-actions.ts, crie-as garantindo que façam o insert nas tabelas pivô (event_drinks, event_labor, event_materials) e terminem com revalidatePath('/events/[id]').

Atualização das Seções do Evento (Os botões e tabelas):

Em event-drinks-section.tsx, event-labor-section.tsx e event-materials-section.tsx:

Adicione o cabeçalho padrão com título e o botão 'Adicionar [Item]' à direita.

O botão deve abrir um Dialog do shadcn contendo o respectivo formulário criado no Passo 1.

Após o onSuccess do formulário, chame router.refresh() e feche o modal para a tabela atualizar automaticamente.

Atalho para a Lista de Compras (event-financial-section.tsx):

Localize a tabela onde está escrito 'Ingredientes (Lista de Compras)'.

Substitua o texto ou adicione um botão de ação na mesma linha: importe o componente Link do next/link e o componente Button do shadcn.

Crie um botão (variante 'outline' ou 'secondary') com o texto 'Ver Lista de Compras' e um ícone de carrinho (ShoppingCart do lucide-react). Envolva-o no Link apontando para a rota /events/${eventId}/shopping-list.

Garantia de Dados:

Certifique-se de que a página principal app/events/[id]/page.tsx está buscando os catálogos completos (drinks, labor, materials) no servidor e passando-os como props para as respectivas seções para alimentar os componentes <Select> dos formulários.

Não crie comentários no código e certifique-se de que os modais de edição e exclusão nas tabelas das seções também estejam funcionando e apontando para as actions corretas."
