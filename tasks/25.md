"Preciso adicionar a métrica de 'Custo por Pessoa' no resumo financeiro do evento.

Encontre o componente EventFinancialSection e o arquivo pai que o renderiza (provavelmente em app/events/[id]/...), e execute as seguintes alterações rigorosamente:

No componente EventFinancialSection:

Adicione guests: number ao type Props.

Desestruture guests e calcule o costPerPerson (guests > 0 ? grandTotal / guests : 0).

Ajuste a grid dos cards no topo para grid-cols-2 lg:grid-cols-4 e adicione um quarto <Card> com título 'Custo por Pessoa' (use a cor de texto text-primary para destacar).

Adicione uma div wrapper na <Table> com className="overflow-x-auto" para garantir o scroll mobile.

Adicione whitespace-nowrap nas TableCell e TableHead para os textos não quebrarem.

No <TableFooter>, adicione uma nova <TableRow> abaixo do 'Gasto Total Estimado' exibindo a 'Média por convidado ({guests} pessoas)' e o valor do costPerPerson.

No arquivo Pai (onde EventFinancialSection é chamado):

Localize a renderização do <EventFinancialSection .../>.

Passe a nova propriedade guests correspondente aos dados do evento atual (ex: guests={event.guests}).

Não crie comentários no código e garanta que todas as tipagens do TypeScript sejam respeitadas."
