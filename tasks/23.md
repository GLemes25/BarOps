action createIngredient e updateIngredient falharam porque o driver neon-http do Drizzle não suporta db.transaction(). Precisamos refatorar as actions de Ingredientes, Drinks e Eventos que estão usando db.transaction.

Sua tarefa é remover os blocos de transação e usar operações sequenciais. Execute rigorosamente:

Em actions/ingredient-actions.ts:

Em createIngredient: Remova o db.transaction. Primeiro, faça o insert em ingredients retornando o id. Guarde esse id. Em seguida, se houver components, faça um try/catch para inserir na ingredient_components. Se o insert dos componentes falhar no catch, execute um await db.delete(ingredients).where(eq(ingredients.id, novoId)) para reverter e jogue o erro pra cima.

Em updateIngredient: Remova a transação. Execute as chamadas sequenciais usando o ID recebido: await db.update(...), depois await db.delete(ingredient_components).where(...), e por último insira os novos components.

Em actions/drink-actions.ts:

Aplique exatamente a mesma lógica (remoção do db.transaction()) para o createDrink e updateDrink, inserindo primeiro o drink, pegando o ID, e depois inserindo na tabela drink_ingredients. Se o insert dos ingredientes falhar no createDrink, delete o drink que acabou de ser criado.

Não crie comentários no código e garanta que as tipagens estejam corretas."
