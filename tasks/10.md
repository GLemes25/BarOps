"A página app/ingredients/page.tsx está atualmente configurada como um Client Component e renderizando um estado inicial vazio. Precisamos conectá-la ao banco de dados Neon DB usando Drizzle e Server Actions, adotando a arquitetura de Colocation (co-localização de componentes dentro da pasta da rota).

Execute a seguinte refatoração rigorosamente de acordo com as .claude/rules:

Server Actions: Em src/actions/ingredient-actions.ts, certifique-se de que existem as funções assíncronas getIngredients (que retorna todos os ingredientes do banco) e deleteIngredient (que recebe o ID, deleta do banco e chama revalidatePath('/ingredients')).

Separação de Responsabilidades (Client Component co-localizado):

Crie um novo arquivo EXATAMENTE na pasta da rota: app/ingredients/ingredients-client.tsx.

Mova toda a lógica de 'use client' atual (importações do React, estados do useState, controle dos Dialogs, a Table e a chamada do formulário) para este novo arquivo.

Altere este componente IngredientsClient para receber uma prop chamada initialData contendo o array de ingredientes tipado. Defina o useState inicial com esse initialData.

Atualize a função handleDelete para ser assíncrona: ela deve chamar a server action deleteIngredient. Após o sucesso da action, utilize useRouter().refresh() do next/navigation para sincronizar a tela com o servidor.

Página de Servidor (Server Component):

Reescreva o arquivo app/ingredients/page.tsx para ser um Server Component (remova o 'use client' e os hooks de estado).

Ele deve ser uma Arrow Function assíncrona (const IngredientsPage = async () => { ... }).

Faça a chamada para await getIngredients() diretamente no corpo do componente.

Importe o client component usando um caminho relativo (import { IngredientsClient } from "./ingredients-client") e renderize-o passando os dados: <IngredientsClient initialData={dadosDoBanco} />.
