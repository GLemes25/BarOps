🚀 Prompt de Inicialização do Agente Local (Dynamic Design Refactor com Economia de Tokens)
Instrução Primária: Fetch this design file, read its readme, and implement the relevant aspects of the design. https://api.anthropic.com/v1/design/h/WmNWQoj4sF-huTl3dGJW1Q?open_file=BarOps+ERP.dc.html

Você está atuando como um Agente Autônomo de Codificação Local (Software Engineering Agent). Seu objetivo é executar um fluxo de trabalho dinâmico (Dynamic Workflow) para refatorar completamente o design visual do ecossistema BarOps, migrando a identidade visual contida no arquivo BarOps ERP.dc.html para os componentes Next.js (App Router) reais do repositório local.

🧠 ESTRATÉGIA DE ECONOMIA DE TOKENS (ROTEAMENTO DE MODELOS)
Para otimizar o consumo de recursos e custos, divida estritamente a execução usando os seguintes modelos:

Fase de Planejamento e Análise: Use o modelo Claude 3 Opus para ler o arquivo de design, analisar a estrutura atual do repositório e traçar o plano de arquitetura de cada componente.

Fase de Execução (Escrita de Código) e Revisão (Testes/Compilação): Mude as instruções dos subagentes para utilizar o modelo Claude 3.5 Sonnet para a alteração em lote dos arquivos e checagem de erros do TypeScript.

🛑 REGRA DE EXECUÇÃO CRÍTICA (LIMITADOR DE SUBAGENTES)
Limite Estrito: Você está autorizado a gerar ou delegar tarefas para no máximo 10 subagentes por execução completa. Planeje suas iterações de forma eficiente para cobrir os arquivos principais dentro deste teto.

📋 FASE 1: Descoberta e Mapeamento de Arquivos (Executado por Opus)
Analise os aspectos relevantes do design fornecido no link e identifique os padrões visuais aplicados no BarOps ERP.dc.html (paleta de cores Tailwind, classes de opacidade 90% em janelas focadas, layouts de tabela, calendário interativo).

Localize no repositório local os componentes equivalentes que precisam receber essa reestilização (ex: app/dashboard/page.tsx, components/drinks-table.tsx, app/events/[id]/page.tsx, ShoppingListSection).

🛠️ FASE 2: Execução Dinâmica da Refatoração (Executado por Sonnet)
Modifique os arquivos locais de forma iterativa, aplicando o novo design minimalista nas seguintes áreas, respeitando o limite de subagentes:

Dashboard: Implemente a nova interface visual e estruture o Calendário Interativo de Eventos (usando estados locais ou componentes de grid), colorindo os blocos conforme o status do evento.

Tabelas e Listas (Drinks/Eventos): Imponha o uso de table-fixed w-full, responsividade mobile oculta (hidden sm:table-cell) e truncamento de texto longo (truncate) para impedir que os botões de ação vazem da tela, mesmo com a barra lateral aberta.

Seleção de Drinks (Vitrine): Refatore para manter o layout de cards com divisão visual estrita (linha divisória física) entre drinks selecionados (topo) e disponíveis (baixo), além da nuvem de badges para variações de drinks.

Lista de Compras: Garanta que os inputs continuem minimalistas/invisíveis, mantendo as lógicas de cores invertidas para finanças (gastar mais que o calculado = vermelho, gastar menos = verde) e as tags estritas de impressão (print:hidden, print:text-black, spans ocultos para inputs).

⚠️ FASE 3: Guardrails e Restrições de Engenharia (Revisado por Sonnet - PROIBIDO QUEBRAR)
Durante a reestilização, você deve preservar 100% das lógicas de negócio e correções anteriores:

Hydration Guard: Não introduza tags <button> aninhadas. Em triggers do Dialog/Dropdown da Base UI, não use o componente <Button> do shadcn internamente; aplique a estilização diretamente no trigger usando a função buttonVariants.

Timezone Offset Guard: Não altere a lógica que concatena T12:00:00 no envio de datas e o formato nativo de 24h (type="time") com step="60" para horários.

Neon HTTP Limit: Não utilize db.transaction() em nenhuma Server Action que você precise ajustar visualmente. Mantenha os await sequenciais.

Zod Coercion: Não reverta a validação inteligente que transforma strings vazias de chaves estrangeiras em null (impedindo que virem 0).

🔄 Modo de Avanço do Agente:
Proceda de forma autônoma e sequencial. Altere um bloco de arquivos por vez, verifique se o TypeScript compila sem erros locais e passe para a próxima tela. Pare imediatamente se atingir o limite de 10 subagentes e apresente o relatório do que foi concluído.
