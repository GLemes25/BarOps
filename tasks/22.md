Estou enfrentando uma falha silenciosa ao salvar ingredientes. A interface conclui o onSubmit, mas os dados (especialmente de sub-receitas) não vão para o banco de dados.

Revise e corrija rigorosamente os seguintes pontos, seguindo as .claude/rules:

Zod Schema (app/ingredients/ingredient-form.tsx):

O purchaseUnit e purchaseCost devem ser opcionais caso isSubRecipe seja true.

Atualize o schema para:
purchaseUnit: z.string().optional().or(z.literal('')),
purchaseCost: z.coerce.number().min(0).optional(),

Garanta que no defaultValues e no formulário esses valores sejam tratados corretamente (use 0 ou "" como fallback).

Server Actions (src/actions/ingredient-actions.ts):

Revise createIngredient e updateIngredient.

Elas DEVEM usar db.transaction(async (tx) => { ... }).

Passo A na transação: Fazer o insert ou update na tabela ingredients e retornar o ID inserido/atualizado (usando .returning({ id: ingredients.id })).

Passo B na transação: Se values.isSubRecipe for true e houver values.components, iterar sobre eles e fazer um tx.insert(ingredientComponents).values(...) usando o ID do ingrediente pai.

No caso do updateIngredient, antes de inserir os components, faça um tx.delete(ingredientComponents).where(eq(ingredientComponents.parentId, id)).

CRÍTICO: Se qualquer erro ocorrer dentro da transação, faça um throw new Error(...) para que o erro suba para o frontend, ou retorne { success: false, error: e.message }.

Tratamento de Erro no Frontend (app/ingredients/ingredient-form.tsx):

No onSubmit, verifique a resposta da action. Se a action retornar erro, exiba um alerta no console ou não chame o onSuccess(). Deixe a aba de log do terminal clara para podermos debugar se o banco rejeitar algo.

Não crie comentários no código e certifique-se de que os imports do Drizzle (eq, db, esquemas) estejam corretos no arquivo de actions
