"Preciso aplicar a arquitetura de Server Components com 'Colocation' de Client Components nas pastas app/drinks, app/events, app/labor e app/materials, seguindo a mesma lógica que faríamos para ingredientes.

Para CADA UMA dessas 4 rotas, execute rigorosamente os seguintes passos, respeitando todas as nossas .claude/rules:

Criação do Componente de Tabela (Client Component co-localizado):

Na pasta da rota respectiva, crie um arquivo com o sufixo -table.tsx (ex: drinks-table.tsx, events-table.tsx).

Mova toda a lógica de 'use client', importações do React, controle dos Dialogs, formulário e renderização da Tabela do page.tsx atual para este novo arquivo.

O componente (ex: DrinksTable) deve receber uma prop initialData contendo os dados tipados. Inicialize o useState com esse valor.

Atualize a função handleDelete para ser assíncrona: ela deve chamar a server action de exclusão correspondente (ex: deleteDrink(id) importada de src/actions/drink-actions.ts). Após o sucesso da action, chame useRouter().refresh() do next/navigation para sincronizar os dados.

Refatoração da Página Principal (Server Component):

Reescreva o arquivo page.tsx da pasta para ser um Server Component (remova o 'use client' e os hooks).

Ele deve ser uma Arrow Function assíncrona.

Chame a server action de leitura correspondente (ex: await getDrinks()) para buscar os dados no banco (Neon DB via Drizzle).

Importe o novo componente de tabela usando caminho relativo (ex: import { DrinksTable } from "./drinks-table") e renderize-o passando os dados: <DrinksTable initialData={dadosDoBanco} />.

Observação Especial para a rota /events: Como a tabela e o formulário de eventos lidarão com datas, certifique-se de aplicar a regra estrita de usar a biblioteca dayjs para qualquer formatação ou manipulação de data.

Não adicione comentários no código gerado."
