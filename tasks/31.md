"Como um Arquiteto Next.js e Drizzle ORM, implemente a funcionalidade de Duplicação de Eventos.

Lembre-se: O projeto usa o driver neon-http, portanto NÃO utilize db.transaction(). As operações devem ser feitas de forma sequencial com await.

Execute rigorosamente os seguintes passos:

Server Action (duplicateEvent em actions/event-actions.ts):

Crie uma função assíncrona que recebe o eventId original.

Busque o evento original. Se não existir, retorne erro.

Insira o novo evento na tabela events. Use os mesmos dados do original (date, guests, durationHours, avgDrinksPerPerson, totalDrinks), mas altere:

name: \${originalEvent.name} copy``

status: 'planning'

revenue: '0'

Retorne o id do novo evento inserido usando .returning({ id: events.id }).

Clonagem das Relações (Filhos):

Busque os registros do evento original nas tabelas event_labor, event_materials e event_drinks.

Para cada tabela, mapeie os dados removendo o id original e substituindo o eventId pelo ID do novo evento recém-criado.

IMPORTANTE: O Drizzle quebra se você fizer um insert com um array vazio. Portanto, verifique o tamanho do array antes de inserir. Exemplo:
if (newLabor.length > 0) await db.insert(eventLabor).values(newLabor);

Faça isso para labor, materials e drinks de forma sequencial.

Retorne { success: true, newEventId }.

Integração na UI (Tabela de Eventos e/ou Detalhes do Evento):

Adicione a opção 'Duplicar Evento' no componente TableRowActions (dropdown menu) da tabela de eventos (ex: events-table.tsx ou similar).

Opcional: Adicione um botão de duplicar no cabeçalho da página de detalhes do evento (app/events/[id]/page.tsx).

Ao clicar, exiba um estado de loading, chame a action duplicateEvent, exiba um toast.success('Evento duplicado!') e faça um router.push(\/events/${result.newEventId}`)` para redirecionar o usuário diretamente para o painel do novo evento copiado.

Faça a implementação de forma limpa, garantindo o tratamento de erros e sem adicionar comentários explicativos no código.
