"A lógica de Variações de Drinks (Parent/Child) precisa ser ajustada para o padrão 'Pasta/SKU'. O Drink Pai deve atuar estritamente como uma 'Pasta' (Agrupador) vazia, sem receita própria, enquanto apenas os Filhos (SKUs) possuem ingredientes e vão para o evento.

Execute as seguintes correções rigorosamente:

Conceito de Pasta no CRUD de Drinks (drink-form.tsx):

Adicione um Switch/Checkbox: 'Este drink é um Agrupador (Pasta vazia para variações)'.

Se isso estiver marcado, oculte completamente o FieldArray de Ingredientes (um agrupador não tem receita). Se desmarcado, mostre os ingredientes. No backend, se for salvo como agrupador, certifique-se de deletar/não salvar ingredientes em drink_ingredients para esse ID.

Correção do Bug de Mudança de Pai (actions/drink-actions.ts):

Corrija a Server Action de Update do Drink. Atualmente, mudar o parentId de um drink filho para outro parentId diferente (ou remover o pai passando null) está falhando ou sendo ignorado. Garanta que o update do parentId seja feito corretamente no banco.

UX de Agrupamento na Tabela do Evento (app/events/[id]/... / event-drinks-section):

Você está correto em salvar apenas os IDs dos Filhos (Variações) na tabela event_drinks. Isso garante a matemática exata da lista de compras.

O Ajuste Visual: Na tabela/lista onde exibimos os drinks alocados no evento, faça um agrupamento (reduce) via JavaScript no frontend ou na query.

Se houver drinks filhos no evento, exiba o NOME DO PAI como a linha principal, e coloque os nomes dos filhos como Badges ou subtítulos abaixo dele (ex: Linha principal: 'Caipifruta de Limão' | Badges: 'Vodka', 'Cachaça').

Os drinks que não têm pai (Independentes) continuam sendo exibidos normalmente em suas próprias linhas.

Não adicione comentários explicativos no código e mantenha a tipagem rígida do Drizzle/Zod."
